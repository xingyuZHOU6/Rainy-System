# 关键参数与纹理通道速查

## 全局 Shader 数据

| 名称 | 写入者 | 含义 |
|---|---|---|
| `_SRS_depth` | `CoverageBase.GetDepth` | 环境深度的一、二阶指数矩；可选 B 为原始深度 |
| `_depthCamMatrix` | `CoverageBase.UpdateDepthCam` | 世界空间到覆盖相机裁剪空间 |
| `_depthCamFarHeight` | `CoverageBase.UpdateDepthCam` | 深度坐标修正使用的高度 |
| `_depthCamDir` | `CoverageBase.UpdateDepthCam` | 降雪方向/坡度遮罩参考方向 |
| `_SRS_TraceTex` | `SRS_TraceMaskGenerator.SetVariables` | RG 法线、B 基础痕迹、A 痕迹高度 |
| `_SRS_TraceSurfCamMatrix` | `SRS_TraceMaskGenerator.SimOneStep` | 世界空间到痕迹相机裁剪空间 |
| `_TraceDetailTex` | `SnowCoverage.UpdateCoverageMaterial` | RG 细节法线，B 细节高度 |
| `_BlueNoise` | `CoverageBase.UpdateCoverageMaterial` | 抖动/随机采样 |

## 覆盖纹理 `_CoverageTex0/1/2`

| 通道 | 用途 |
|---|---|
| R,G | 法线 XY |
| B | 雪高度/位移形状 |
| A | 光滑度；也可按配置作为闪光遮罩来源 |

## 痕迹纹理 `_SRS_TraceTex`

| 通道 | 用途 |
|---|---|
| R,G | 由高度有限差分重建、编码到 0..1 的法线 XY |
| B | 基础接触遮罩，Composite Pass 中乘 10 后写入 |
| A | 最终高度场；未压雪面以 0.5 为基准，边缘、噪声和 indent 会改变它 |

## 最常调的参数

| 参数 | 现象 | 建议 |
|---|---|---|
| `areaSize` | 可交互世界范围 | 越大越省跟随次数，但相同分辨率下精度更低 |
| `depthTextureResolution` | 屋檐/积雪遮挡精度 | 先 512 或 1024，观察成本后再升 |
| `maskRes` | 脚印平面分辨率 | 1024 是样例值；2048 像素成本约 4 倍 |
| `depthBias` | 接触是否触发/穿透厚度 | 无痕迹时小步调；过大会让悬空物也压雪 |
| `vertexPush` | 代理网格膨胀 | 薄鞋底或低模接触不稳定时增加 |
| `blurKernelSize` | 痕迹边缘软硬 | 越大越软、采样越贵 |
| `normalSpread` | 痕迹法线坡度采样距离 | 小值更锐，大值更宽缓 |
| `sourceMaskBrightness` | 接触遮罩强化 | 提高会更快形成完整脚印 |
| `borderIntensity` | 压痕边缘隆起 | 0 关闭边缘环 |
| `indent` | 凹陷强度 | 1 使用完整凹陷造型 |
| `decaySpeed` | 雪痕恢复速度 | 0 永久保留；正值逐帧恢复 |
| `coverageDisplacement` | 雪层几何厚度 | 需位移 keyword 与足够顶点密度 |
| `tessEdgeL` | 细分目标边长 | 越小细分越密、成本越高 |

