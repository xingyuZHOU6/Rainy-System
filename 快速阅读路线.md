# 快速阅读路线

## 30 分钟：抓住交互脚印主线

1. `SRS_TraceMaskGenerator.cs`：先看字段、`CreateStuff`、`SetupCam`、`SimOneStep`。
2. `TraceMaskGen.shader`：依次看三个 Pass；暂时跳过 Simplex Noise 公式。
3. `SRS_Tracer.cs`：理解为什么需要代理网格和独立 Layer。
4. `SRS_SnowCoverage.hlsl`：只读 `#ifdef _TRACES_ON` 两段，一段在 `SnowCoverage`，一段在 `Displace`。

读完应能回答：两张深度图如何表示接触？历史痕迹存在哪？RGBA 四通道怎样改变雪？

## 2 小时：理解完整积雪与位移

1. `CoverageBase.cs`：`InitTexturesAndCam` -> `UpdateSRSPosition` -> `GetDepth` -> `ConvertDepthToVSM`。
2. `SRS_RenderDepthWithReplacement.cs` 与 `SRS_DepthRenderer.asset`。
3. `TexturePacking.shader`、`NL_GaussianBlur.shader`。
4. `SRS_CoverageCommon.hlsl`：`chebyshevUpperBound`、`ComputeBasicAreaMask`、`DirMask`、`CalcVolumeMask`。
5. `SnowCoverage.cs`：Inspector 参数如何转为材质属性和 keyword。
6. `SRS_SnowCoverage.hlsl`：`SnowCoverage` 与 `Displace`。
7. `SRS_Tessellation.hlsl` 和一个 Tessellation Shader 入口。

## 半天：理解 URP 多 Pass 与 Terrain

1. `SnowCoverage.shader` / `SnowCoverage (Tessellation).shader`：Properties、keyword、Forward/GBuffer/Depth/Shadow/DepthNormals。
2. `SRS_LitInputSnow.hlsl`：材质 CBUFFER、纹理与采样器声明。
3. `SRS_LitForwardPass.hlsl` 和 `SRS_LitGBufferPass.hlsl`。
4. `SRS_DepthOnlyPass.hlsl`、`SRS_LitDepthNormalsPass.hlsl`、`SRS_ShadowCasterPass.hlsl`。
5. Terrain 的 Input -> LitPasses -> 各专用 Pass -> Terrain Shader 入口。
6. `SRS_TerrainUtility.cs`：远景贴图烘焙和位移包围盒。

## 一天：扩展与工具

- 雪光照：`SRS_LightingCommon.hlsl`、`SRS_Lighting.hlsl`。
- 世界空间投影：`SRS_Triplanar.hlsl`。
- Inspector/材质覆盖机制：`SnowCoverage_editor.cs`、`SnowShadersGUI.cs`、`SRS_CoverageShaderGUI_base.cs`。
- 可绘制覆盖：TotalBrush 的 `NL_TotalBrush.cs`、`NL_PaintableObject.cs`、Compute Shader。
- GPU 飘雪：`SRS_ParticleSystem.cs` 与两个粒子 Shader；它是邻接视觉系统，不参与脚印生成。
- Built-in 管线路径：`CGIncludes`；当前项目实际运行的是 URP/HLSL 路径。

## 源码导航提示

在 `02_中文注释源码` 全局搜索以下标记：

- `【中文注释】`：关键行内说明。
- `_SRS_depth`：环境遮挡矩纹理的完整生产/消费链。
- `_SRS_TraceTex`：痕迹纹理的完整生产/消费链。
- `_TRACES_ON`：交互痕迹的 Shader 分支。
- `_DISPLACEMENT_ON`：几何位移分支。
- `_SRS_TraceSurfCamMatrix`：世界坐标到痕迹 UV 的变换。
- `Graphics.Blit`：每个 GPU 后处理阶段。

