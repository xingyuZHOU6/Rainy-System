# 可交互雪技术原理详解

## 1. 总体架构

```text
                         CoverageBase / SnowCoverage
                                   |
              +--------------------+--------------------+
              |                                         |
      场景积雪遮挡链路                              动态痕迹链路
              |                                         |
  SRS_InstanceDepthCam（顶视正交）        SRS_Tracer 创建低成本深度代理
              |                                         |
  URP RendererFeature + DepthRenderer       traceObjsCam（从下向上拍代理）
              |                              traceSurfCam（从上向下拍雪面）
          sceneDepthTex                               |
              |                             两张 Depth RT 做接触/穿透比较
  TexturePacking：指数深度的一、二阶矩                |
              |                           上一帧痕迹 + UV 平移 + 衰减
  Gaussian Blur（RG）                                 |
              |                           Blur -> 细节/边缘 -> 法线重建
      全局纹理 `_SRS_depth`                            |
              +--------------------+--------------------+
                                   |
                         `_SRS_TraceTex`（RGBA）
                                   |
                     SRS_SnowCoverage.hlsl
                 颜色 / 光滑度 / 法线 / 顶点位移
                                   |
       Forward、GBuffer、Shadow、Depth、DepthNormals 各 Pass
```

系统的关键设计是把“雪的外观”和“交互历史”都放在 GPU 纹理中。CPU 负责相机、RenderTexture、材质参数和生命周期；GPU 负责深度比较、模糊、噪声、法线重建、三平面采样、位移与 PBR 光照。

## 2. 第一条链：哪里可以积雪

`CoverageBase` 在雪区域上方创建 `SRS_InstanceDepthCam`。它是正交相机，覆盖一个 `areaSize × areaSize × areaDepth` 的盒状体积。相机通过专用 URP Renderer `SRS_DepthRenderer.asset` 渲染，Renderer Feature `SRS_RenderDepthWithReplacement` 把可见物体统一换成 `DepthRenderer.shader`，因此目标只保存几何深度。

原始深度不能直接做柔和遮挡。`TexturePacking.shader` 的第 0 个 Pass 先对深度做指数变换，再输出：

- R：指数深度的一阶矩 `E[x]`
- G：指数深度的二阶矩 `E[x²]`

随后 `NL_GaussianBlur.shader` 对 RG 做横向、纵向模糊。`SRS_CoverageCommon.hlsl` 中的 `chebyshevUpperBound` 和 `ComputeBasicAreaMask` 用矩、方差和切比雪夫上界重建一个柔和遮挡概率。这相当于单侧指数变换的 Variance Shadow Map 思路，用于判断某世界位置是否被屋顶等物体挡住，从而决定雪能否落到这里。

`_CoverageAreaBias` 控制深度偏差，减轻自遮挡；`_CoverageLeakReduction` 压缩概率尾部，减少漏雪；`blurKernelSize` 让边界更软，但核越大 GPU 采样越多。

## 3. 覆盖体积如何跟随玩家

`CoverageBase.UpdateSRSPosition` 不会让体积连续漂移，而是按一个深度纹素对应的世界尺寸对 X/Z 位置取整：

```text
worldTexelSize = areaSize / depthTextureResolution
snappedX = floor(targetX / worldTexelSize) * worldTexelSize
snappedZ = floor(targetZ / worldTexelSize) * worldTexelSize
```

这样相机移动时世界位置不会在深度纹理的像素间持续滑动，能显著降低遮挡和痕迹抖动。体积移动量通过 `onVolumeTeleported` 事件传给痕迹系统，后者把历史纹理的 UV 反向平移，尽量保持脚印锁定在世界空间。

`updateDistanceThreshold` 决定走多远才重新对齐，`updateRate` 决定检查频率。面积越大而分辨率不变，单个纹素代表的世界范围越大，细节越粗。

## 4. 第二条链：如何判断物体压进雪里

### 4.1 深度代理

给动态物体挂 `SRS_Tracer` 后，脚本会为 MeshRenderer 或 SkinnedMeshRenderer 创建一个代理 Renderer，复用网格/骨骼，但把材质替换成 `DepthOccluder.shader`。代理放到指定交互层，只参与痕迹深度相机，不需要真实颜色和光照。

`vertexPush` 可以沿法线扩大代理，弥补低模、鞋底太薄或深度精度带来的接触丢失。SkinnedMesh 代理复用 bones/rootBone，因此动画脚、角色身体也能参与深度写入。

这里有一个容易看错的 URP 细节：`DepthOccluder.shader` 自己处于 Transparent/Overlay 队列并开启 `ZWrite`，所以它不会被 `SRS_RenderDepthWithReplacement` 的 opaque 过滤阶段替换，而会在专用 Renderer 的透明阶段用自身 Shader 写深度。因此 `_VertexPush` 的顶点扩张仍然有效；Replacement Shader 主要处理普通不透明雪面/几何体。

### 4.2 两台相反方向的正交相机

`SRS_TraceMaskGenerator` 建立两台相机：

- `traceSurfCam`：位于体积上方，从上向下拍“可被压的雪面层”，写入 `surfaceDepthRT`。
- `traceObjsCam`：作为前者的子相机移动到体积底部，旋转 180° 从下向上拍交互代理层，写入 `traceObjsRT`。

因为两台相机从相反方向测量，Shader 把平台的 reversed-Z 差异统一后，用 `surfaceDepth - traceObjectDepth` 估算代理进入雪面的程度。它不是 Physics 碰撞求交，而是屏幕空间/区域空间的 GPU 深度场求交，所以能同时处理大量像素级接触。

### 4.3 历史、衰减和边界

`TraceMaskGen.shader` 的 `Primary Mask` Pass 同时读取：

- 本帧雪面深度；
- 本帧交互物深度；
- 上一帧 `traceMaskRT`。

新接触和旧痕迹取较大值，再限制在覆盖体积边界内。`_DecaySpeed` 按 `unity_DeltaTime.x` 让历史逐步回到基线；体积移动时 `_UVOffset` 滚动历史采样位置。样例把 `decaySpeed` 设为 0，因此痕迹不会自动恢复。

之后执行 R 通道的两遍高斯模糊，以消除深度锯齿并形成连续凹坑。

### 4.4 细节与法线重建

`Detail Mask` Pass 在平滑接触遮罩上加入 Simplex Noise、凸起边缘和凹陷强度。`Composite Mask` Pass 对最终高度在左右上下各采样一次，用有限差分和叉乘重建法线，最后打包到 `traceTex`。

`_SRS_TraceTex` 通道约定：

| 通道 | 含义 | 主要消费者 |
|---|---|---|
| R,G | 0..1 编码的痕迹法线 X/Y | 像素阶段重建 `traceNormals` |
| B | 平滑的基础接触遮罩，写出时乘 10 | 痕迹颜色/细节影响范围 |
| A | 带边缘与噪声的痕迹高度，未压处约 0.5 | 顶点位移与痕迹深浅 |

## 5. 雪材质怎样消费这些数据

`SRS_SnowCoverage.hlsl` 是 URP 路径的核心。

### 5.1 世界空间定位

片元或顶点的世界坐标乘 `_depthCamMatrix` / `_SRS_TraceSurfCamMatrix`，变换到两个正交覆盖相机的裁剪空间，再映射到 0..1 UV。这样所有雪材质共享同一组世界空间覆盖图，不需要给每个物体单独展开痕迹 UV。

### 5.2 基础积雪

雪层纹理使用世界空间三平面投影，避免普通模型必须拥有统一 UV。默认打包纹理的约定是：

- R,G：雪法线的两个分量；
- B：雪高度；
- A：雪光滑度。

`DirMask` 用表面法线和降雪方向的点积控制坡面是否积雪；环境深度矩控制屋檐遮挡；高度图与 `coverageAmount` 控制雪层过渡；可绘制遮罩可强制添加或擦除覆盖。

### 5.3 痕迹着色

启用 `_TRACES_ON` 后，像素阶段采样 `_SRS_TraceTex`：

- RG 与雪面法线做 Reoriented Normal Mapping 混合；
- B/A 决定痕迹颜色、露出底材的比例和细节法线强度；
- `_TraceDetailTex` 可在世界 XZ 上再叠一层压实雪细节。

### 5.4 真正的几何位移

`Displace` 在顶点阶段重新计算基础雪高度和痕迹高度。未压区域沿世界法线抬高，痕迹区域由 A 通道压低。普通 Shader 只能移动已有顶点；想要清晰脚印轮廓，需要高密度网格，或启用 Tessellation 版本。

`SRS_Tessellation.hlsl` 在 Hull Shader 中按边长、距离、雪量决定细分因子，在 Domain Shader 中插值顶点后再调用位移。阴影、Depth、DepthNormals、Forward、GBuffer 都有相应位移路径，否则画面、阴影和深度会互相穿帮。

## 6. Terrain 路径

Terrain 不能直接复用普通 Mesh Lit Pass，因此系统复制并扩展了 URP Terrain Lit 的输入和 Pass：

- Terrain splat/control map 先混合底层地表；
- 再调用相同的雪覆盖、痕迹和位移函数；
- Add Pass 处理超过四层的附加 TerrainLayer；
- Tessellation 版本提高近景顶点密度；
- `SRS_TerrainUtility` 烘焙远景 Albedo/Normal，近远切换降低远处成本；
- `patchBoundsMultiplier` 扩大 Terrain patch 包围盒，避免位移后的顶点被错误剔除。

## 7. 可绘制覆盖与 TotalBrush

运行时痕迹和“美术预先画好的积雪分布”是两套不同机制。

- Mesh：使用顶点色。R 通道表达添加/擦除覆盖，G/B/A 可作为三层雪纹理权重。
- Terrain：使用 `_PaintedMask` 与 `_PaintedMaskNormal` 纹理。
- `PaintMaskRGBA` 以 0.5 为分界，把通道解释为擦除或添加强度。
- TotalBrush 目录提供 SceneView 工具、Compute Shader 和绘制 Shader，用来生成这些顶点色/纹理；它不负责每帧脚印历史。

同属扩展接入工具的 `BatchShaderSwapper.cs` 是 EditorWindow：它可以扫描选中对象、当前场景或整个项目的材质，并批量替换为 Weatherade Snow Coverage Shader。它只帮助已有材质接入雪系统，不参与运行时深度、痕迹或位移计算。

## 8. URP 接入点

专用 `SRS_DepthRenderer.asset` 的普通 Opaque/Transparent LayerMask 被设为 0，只依靠自定义 Renderer Feature 绘制替换深度。这可避免先正常渲染一遍场景再叠加替换 Pass。

Unity 2022 / URP 14 走 `ScriptableRenderPass.Execute` 兼容路径；Unity 6 分支使用 RenderGraph，并通过 `SRS_DataTransfer` 保存 Camera -> RTHandle 映射，把外部 RenderTexture 作为深度附件导入。`URP_RendererSetter` 通过反射访问 URP Asset 的私有 `m_RendererDataList`，自动追加专用 Renderer；这是方便但版本敏感的实现。

## 9. 主要 RenderTexture 与成本

| 纹理 | 格式/尺寸 | 更新 | 用途 |
|---|---|---|---|
| `sceneDepthTex` | Depth，coverageResolution | 体积初始化或移动 | 环境遮挡深度 |
| `texBlured` | RGFloat/RGHalf | 同上 | 指数深度的一、二阶矩 |
| `traceObjsRT` | Depth，maskRes | 每帧 | 动态代理深度 |
| `surfaceDepthRT` | Depth，maskRes/2 | 每帧 | 雪面深度 |
| `traceMaskRT` | R16，maskRes | 每帧且保留历史 | 基础痕迹 |
| `bluredRT` | R16，maskRes | 每帧 | 平滑痕迹 |
| `finalTraceMask` | R16，maskRes | 每帧 | 带边缘/噪声的高度 |
| `traceTex` | ARGB32，maskRes | 每帧 | 法线 + 基础遮罩 + 高度 |

真正的热点通常是两台痕迹相机、多个全屏 Blit 和高分辨率曲面细分。分辨率从 1024 提升到 2048，像素工作量约变为 4 倍；高斯核也会线性增加纹理采样。

## 10. 快速复现最小系统

1. 项目启用 URP，并把 `SRS_DepthRenderer.asset` 加入当前 URP Asset 的 Renderer 列表。
2. 创建 `SnowCoverage` 对象，配置覆盖面积、深度层和跟随目标。
3. 同一对象添加 `SRS_TraceMaskGenerator`，分别设置雪面层与交互代理层。
4. 雪面材质使用 Weatherade Snow Coverage Shader，启用 Coverage、Traces，若要真实凹陷再启用 Displacement/Tessellation。
5. 让雪面 Renderer 进入 `traceSurfLayermask`。
6. 给鞋、轮胎、箱子等对象挂 `SRS_Tracer`，确保代理层在 `traceObjsLayermask` 中。
7. 运行时观察 `_SRS_depth` 与 `_SRS_TraceTex`；先确认 B/A 痕迹出现，再检查法线和位移。

## 11. 调试顺序

1. 专用深度相机是否真的使用 `SRS_DepthRenderer`。
2. 两个 LayerMask 是否分别看到雪面和动态代理。
3. `traceObjsRT` / `surfaceDepthRT` 深度方向是否正确。
4. `traceMaskRT` 是否出现接触，若没有先调 `depthBias`、`vertexPush`。
5. `traceTex.B/A` 是否变化。
6. 材质 `_TRACES_ON` 是否启用，`_SRS_TraceTex` 是否为全局纹理。
7. 只有法线没有凹陷时，检查 `_DISPLACEMENT_ON`、网格密度和 Tessellation Shader。
8. 移动后脚印漂移时，检查体积 snap、`_UVOffset` 和 areaSize 是否一致。

## 12. 阅读时值得留意的实现风险

- `URP_RendererSetter` 使用 URP 私有字段反射，升级 URP 时需要验证。
- `SRS_TraceMaskGenerator.cs` 顶部直接 `using UnityEditor;`，没有完全包在 `#if UNITY_EDITOR` 内；做 Player Build 前应核对当前导入版本/程序集设置。
- 痕迹生成器在重建资源时创建多张 RT 和 Material，源码里释放路径不完整；频繁改分辨率或重复 Init 时建议用 Profiler 检查显存。
- `SRS_Tracer` 使用 `renderer.materials`，会实例化材质；代理对象也需要明确销毁策略。
- `SRS_Tracer.SetupTracers` 中重置旋转/缩放的对象写成了原组件 `transform`，阅读移植时应特别核对是否本意为 `tracerObj.transform`。
- 代码用 `projectionMatrix * worldToCameraMatrix`，而 `GL.GetGPUProjectionMatrix` 版本被注释；跨图形 API 时要实测 UV/Y 翻转和 reversed-Z。
- 单例 `CoverageBase.instance` 意味着默认只支持一个雪/雨覆盖控制器。
