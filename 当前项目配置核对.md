# 当前项目配置核对（重要）

这部分是对“当前工作目录里的实际配置”做的静态核对，不是对源码机制的猜测。源码副本没有替你修改这些设置。

## 1. 当前启用的 URP Asset 缺少第 2 个 Renderer

当前 `ProjectSettings/GraphicsSettings.asset` 和活动 Quality 配置引用的是：

```text
Assets/Settings/URP-HighFidelity.asset
```

这个 URP Asset 的 `m_RendererDataList` 只有一个元素，合法 Renderer Index 只有 0。

但是 `BasicSetupSnow.unity` 中的 SRS 深度/痕迹相机序列化为 `m_RendererIndex: 1`，`SnowCoverage.srsRendererId` 也保存为 1。包自带的下列样例 URP Asset 才含两个 Renderer，Index 1 指向专用 `SRS_DepthRenderer.asset`：

```text
Assets/NOT_Lonely/Weatherade SRS/Samples/SharedContent/URP Settings/URP_high_settings.asset
```

这意味着直接用当前全局 URP 配置打开样例时，专用替换深度 Renderer 链很可能不完整。研究/调试时应先在 URP Asset Inspector 核对 Renderer List，确保 `SRS_DepthRenderer` 确实位于组件所用的索引，或让 `URP_RendererSetter` 在编辑器中正确追加后再保存配置。

## 2. 当前 TagManager 没有自定义 Layer 名

`ProjectSettings/TagManager.asset` 的 Layer 8–31 当前全部为空；但：

- `SRS_Tracer` 默认调用 `LayerMask.NameToLayer("Dynamic")` 创建代理；
- 样例场景的痕迹表面 LayerMask 使用数值层 11；
- 动态痕迹对象 LayerMask 包含数值层 12 和 23；
- 样例 Prefab 中有对象直接序列化在这些数字层上。

若 `Dynamic` 未定义，`NameToLayer` 会返回 -1，新建 tracer 代理的层设置将无效/报错。需要在 TagManager 恢复明确的层名，并让：

1. 可变形雪面进入 `traceSurfLayermask`；
2. 鞋底、轮胎、箱子代理进入 `traceObjsLayermask`；
3. 两者不要因 Layer 配错而被同一台相机错误捕获。

仅从当前残缺的 TagManager 不能可靠还原原作者给 11/12/23 的具体名称，所以本包不擅自替你命名。

## 3. 样例的功能开关由材质 Override 接管

`BasicSetupSnow.unity` 的 `SnowCoverage` 全局序列化值里，Traces、Displacement、Tessellation 都是关闭值；但 `Terrain_snow.mat` 中：

```text
_Traces = 1,       _TracesOverride = 1
_Displacement = 1, _DisplacementOverride = 1
_Tessellation = 1, _TessellationOverride = 1
```

`Override = 1` 的含义是该材质拒绝接收全局控制器对此属性的覆盖，因此 Terrain 仍会单独开启交互痕迹、位移和细分。阅读 `CoverageBase.SetFloat/SetKeyword` 与 `SnowCoverage.UpdateCoverageMaterial` 时要牢记这一层优先级，否则会误以为场景全局开关为 0 就不可能有脚印。

## 4. 构建与升级前的源码注意项

- `SRS_TraceMaskGenerator.cs` 顶部直接引用 `UnityEditor`，建议在 Player Build 前实测，必要时将该 using 放入 `#if UNITY_EDITOR`。
- `Scripts/Extra/NL_Utilities.cs` 同样无条件 `using UnityEditor`；虽然多数 Editor API 方法体位于条件编译中，这条 using 本身仍应在 Player Build 前处理/验证。
- `URP_RendererSetter` 反射访问 URP 私有字段 `m_RendererDataList`，升级 URP/Unity 后必须重新验证。
- `URP_RendererSetter` 会尝试在编辑状态自动追加 `SRS_DepthRenderer`，但源码没有显式 `SetDirty/SaveAssets`；当前磁盘上的 URP Asset 仍只有 Renderer 0，不能把一次编辑器内存中的成功追加当成已持久化配置。
- Unity 2022.3 + URP 14 走传统 `ScriptableRenderPass.Execute`；Unity 6 条件分支走 RenderGraph，两条路径都在源码包中。
- 当前脚本宏在 Standalone 下为 `WEATHERADE_INCLUDED;USING_URP`。迁移到新项目时需要同步宏或改用 asmdef/version define。
- 当前 Build Settings 只登记了 `Assets/Scenes/SampleScene.unity`，交互雪样例 `BasicSetupSnow.unity` 没有进入构建列表；研究时应直接打开样例场景，准备出包时再按需要加入。
